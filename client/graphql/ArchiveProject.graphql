mutation ArchiveProject($inputs: ArchiveProjectInputs!) {
  archiveProject(inputs: $inputs) {
    data {
      archivedAt
      id
      status
    }
    errors {
      field
      message
    }
  }
}
